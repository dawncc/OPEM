# Onevom Sites showcase

This directory contains the Cloudflare Worker-compatible Sites presentation layer for Onevom — One Personal Evolving Memory System.

It is intentionally read-only: the FastAPI API, Worker, and SQLite/PostgreSQL database remain independently deployed inside the user's LAN or private network.

```bash
pnpm install
pnpm run check
```
